{
  "name": "{{EVENT_ID}}",
  "version": "1.0.0",
  "description": "Virtual Exhibition Instance for {{EVENT_TITLE}}",
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB={{D1_DATABASE_NAME}} --local",
    "init-db:local": "wrangler d1 execute {{D1_DATABASE_NAME}} --file=schema.sql --local",
    "init-db:remote": "wrangler d1 execute {{D1_DATABASE_NAME}} --file=schema.sql --remote",
    "deploy": "wrangler pages deploy public"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
